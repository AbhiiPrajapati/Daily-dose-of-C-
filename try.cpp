#include <iostream>
using namespace std;
int main()
{
  int num = 1, i;
  for (int k = 0; k <= i; k++)
  {
    cout << number << " ";
    num = num * (i - k) / (k + 1);
  }
  cout << endl;
  return 0;
}
